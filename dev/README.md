# Dev Area

This tree is for contributor-local and repo-maintenance helpers that are not
part of the published library surface.

- Library code stays under `packages/`.
- Public operator docs stay under `docs/`.
- Local prompt packs and other personal AI/session material stay ignored under
  `dev/prompts/`.
